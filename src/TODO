prisera zomrie na udery nie automaticky
passivky
attribute

[CHOOSE.ATTRIBUTE]: {
    getEntities: (images) =>
      images.map((im, i) => ({ ...im, isEnabled: i === 0 ? () => false : im.isEnabled })),
  },
